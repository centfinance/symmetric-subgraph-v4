{
  "name": "@subgraphs/v3-vault",
  "private": true,
  "scripts": {
    "codegen": "graph codegen subgraph.moksha.yaml --output-dir src/types/ && graph build subgraph.moksha.yaml",
    "deploy": "graph deploy",
    "build": "graph build",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.90.0",
    "@graphprotocol/graph-ts": "0.35.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
