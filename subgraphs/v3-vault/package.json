{
  "name": "@subgraphs/v3-vault",
  "private": true,
  "scripts": {
    "codegen:moksha": "graph codegen subgraph.moksha.yaml --output-dir src/types/ && graph build subgraph.moksha.yaml",
    "codegen:sepolia": "graph codegen subgraph.sepolia.yaml --output-dir src/types/ && graph build subgraph.sepolia.yaml",
    "codegen:swellchain-sepolia": "graph codegen subgraph.swellchain-sepolia.yaml --output-dir src/types/ && graph build subgraph.swellchain-sepolia.yaml",
    "codegen:swell-testnet": "graph codegen subgraph.swell-testnet.yaml --output-dir src/types/ && graph build subgraph.swell-testnet.yaml",
    "deploy": "graph deploy",
    "deploy:moksha": "graph deploy symmetric-v-4-moksha-vault subgraph.moksha.yaml",
    "deploy:sepolia": "graph deploy symmetric-v-4-sepolia-vault subgraph.sepolia.yaml",
    "deploy:swellchain-sepolia": "graph deploy symmetric-v-4-swellchain-sepolia-vault subgraph.swellchain-sepolia.yaml",
    "deploy:goldsky:swell-testnet": "pnpm codegen:swell-testnet && node ../../scripts/deploy-goldsky.js --dir . --manifest subgraph.swell-testnet.yaml --subgraph symmetric-v-4-swell-testnet-vault --tag swell-testnet",
    "build": "graph build",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.90.0",
    "@graphprotocol/graph-ts": "0.35.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
