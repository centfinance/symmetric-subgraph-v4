{
  "name": "@subgraphs/v3-pools",
  "private": true,
  "scripts": {
    "add-pool": "node scripts/cli.js",
    "add-factory": "node scripts/cli.js",
    "codegen:moksha": "graph codegen subgraph.moksha.yaml --output-dir src/types/ && graph build subgraph.moksha.yaml",
    "codegen:sepolia": "graph codegen subgraph.sepolia.yaml --output-dir src/types/ && graph build subgraph.sepolia.yaml",
    "codegen:swellchain-sepolia": "graph codegen subgraph.swellchain-sepolia.yaml --output-dir src/types/ && graph build subgraph.swellchain-sepolia.yaml",
    "codegen:swell-testnet": "graph codegen subgraph.swell-testnet.yaml --output-dir src/types/ && graph build subgraph.swell-testnet.yaml",
    "deploy": "graph deploy",
    "deploy:moksha": "graph deploy symmetric-v-4-moksha-pools subgraph.moksha.yaml",
    "deploy:sepolia": "graph deploy symmetric-v-4-sepolia-pools subgraph.sepolia.yaml",
    "deploy:swellchain-sepolia": "graph deploy symmetric-v-4-swellchain-sepolia-pools subgraph.swellchain-sepolia.yaml",
    "deploy:goldsky:swell-testnet": "pnpm codegen:swell-testnet && node ../../scripts/deploy-goldsky.js --dir . --manifest subgraph.swell-testnet.yaml --subgraph symmetric-v-4-swell-testnet-pools --tag swell-testnet",
    "test": "graph test",
    "build": "graph build"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.90.0",
    "@graphprotocol/graph-ts": "0.35.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
